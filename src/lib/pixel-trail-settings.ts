export const DEFAULT_PIXEL_TRAIL_SETTINGS = {
  fullyRevealed: false,
  imageScale: 0.48,
  pixelSize: 36,
  radius: 58,
  fadeDuration: 900,
  maxPixels: 84,
  initialPixels: 24,
}

export type PixelTrailSettings = typeof DEFAULT_PIXEL_TRAIL_SETTINGS
